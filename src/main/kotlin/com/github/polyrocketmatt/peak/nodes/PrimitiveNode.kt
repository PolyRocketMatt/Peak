package com.github.polyrocketmatt.peak.nodes

/**
 * A primitive-node is a node in the procedural generation
 * pipeline that describes some primitive shape. They are
 * the first nodes in any pipeline-chain.
 */
interface PrimitiveNode